const initialState={
    city:'',
    amountOfTime:'',
    preference:[],
}

export const pointReducer=(state=initialState, action)=>{
    switch (action.type) {
        default:
            return state;
    }
}